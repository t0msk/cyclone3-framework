#!/bin/perl
# áéíóú - USE UTF-8 !!!
package Tomahawk::module;
use open ':utf8', ':std';
use encoding 'utf8';
use utf8;
use strict;
#use a400;

sub execute
{
	my %env=@_;
	Tomahawk::GetXSGN(-convertvars=>1) || return undef; # NATIAHNEM XML DESIGN

	#$env{hlt} =~ s|_.*||; # lokalna zalezitost 3domu
	$XSGN{TMP} =~ s|<%HLT_$env{hlt}%>|$XSGN{HLT}|;

	#main::_log("xt_IDcat: ");
	# transformacia hlt
=head1
	my %TRANSFORM = (
		'architecture' => 1,
		'virtual-reality' => 2,
		'archiacademy' => 3,
		'about-us' => 4,
		'links' => 5,
		'contact' => 6,
	);
=cut

	#$env{xt_IDcat} =~ /(....)/; $env{xt_IDcat} = $1;
	#$env{hlt} = $TRANSFORM{$env{hlt}};
	
	#$XSGN{TMP} =~ s|<%HLT_$TRANSFORM{$env{hlt}}%>|$XSGN{HLT}| unless $XSGN{TMP} =~ s|<%HLT_$env{hlt}%>|$XSGN{HLT}|;

	return 1;
 
}

1;

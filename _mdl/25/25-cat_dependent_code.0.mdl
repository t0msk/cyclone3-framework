#!/bin/perl
# USE UTF-8 !!!
package Tomahawk::module;
use open ':utf8', ':std';
use encoding 'utf8';
use utf8;
use strict;

sub execute
{
	my %env=@_;
	return 1 unless $env{allow_cat};
	$env{allow_cat}.=";" unless ($env{allow_cat}=~/;$/);
	$env{allow_cat}=";".$env{allow_cat} unless ($env{allow_cut}=~/^;/);

	main::_log("actual IDcategory - $env{IDcategory}");

	if($env{allow_cat}=~/;$env{IDcategory};/)
	{
		Tomahawk::GetXSGN(-convertvars=>1) || return undef;
		main::_log("category allowed. inserting code.");
	} # NATIAHNEM XML DESIGN

	return 1;

}

1;